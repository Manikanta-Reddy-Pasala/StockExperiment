[pytest]
# Collect only the active test suite. _archived_models holds retired models
# (e.g. ORB, archived 2026-06-05) whose tests import now-moved modules.
testpaths = tests
norecursedirs = _archived_models venv .git __pycache__ node_modules
